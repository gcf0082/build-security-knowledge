---
name: build-security-knowledge
description: 分析代码仓，生成安全测试所需知识库（目录树）
---

# build-security-knowledge

面向代码仓库生成一份安全测试知识库，产物为当前目录下的 Markdown 目录树。它帮助测试者在不读完整个仓库的情况下，快速识别攻击面、绕过点与关键配置位置。

## 适用时机

- 需要对陌生仓库进行安全测试或审计时。
- 需要快速建立对仓库认证、鉴权、限流、沙箱、网关等机制的整体认知时。
- 需要区分外部接口与内部接口、定位 URL 到代码映射时。

## 输入

当前代码仓库的文件树、源码与配置文件。

## 输出

一个 `_security-knowledge/` 目录，采用多级 Markdown 目录树：

```
_security-knowledge/
├── README.md            # 总览：技术栈、覆盖情况、测试者速览
└── <topic>/             # 主题目录，依据仓库实际内容动态确定，如 auth、sandbox、gateway
    ├── README.md        # 主题结论：规律、例外、单点事实
    └── <scope>.md       # 主题下的拆分文件
```

目录层级与主题不预设固定结构，完全依据仓库实际情况决定。

## 执行步骤

1. **扫描仓库**：识别与安全测试相关的主题，例如认证、鉴权、限流、沙箱、网关、白名单等。
2. **提炼规律**：对每个主题，优先用可复用的规则或机制描述它，避免列出具体接口、URL 或文件。
3. **界定例外**：用明确的边界条件说明规律覆盖不到的情况。
4. **标注单点事实**：对既无法归入规律、也不构成例外的孤立实现，单独说明其位置与影响。
5. **生成目录树**：按 `_security-knowledge/` 结构输出。

## 关键概念

- **规律 / 模式**：可被复用的规则或机制。例如：
  - “所有对外接口统一在 `GatewayFilter` 中校验 Token。”
  - “沙箱通过 `seccomp` 限制系统调用。”
- **例外清单**：规律覆盖不到的特例。必须说明其位置、触发条件与原因。例如：
  - “`/internal/health` 在 `GatewayFilter` 中被跳过，因为它匹配 `PERMIT_ALL` 路径列表（见 `SecurityConfig.java:42`）。”
- **单点事实**：无法抽象为规律，且不构成例外的孤立实现。例如：
  - “`LegacyReportController.export` 使用独立的签名验证逻辑，与其他接口不同。”

## 原则

- **普适性**：规律须覆盖某一整类；例外清单界定其边界；无法归类的单点事实单独标注。
- **针对性**：仅记录安全测试所需信息，不复制通用文档或代码注释。
- **可推导性**：描述应让测试者能够基于规律推导出同类行为，而不是背诵一份接口清单。

## 质量检查点

生成完成后，逐项确认：

- [ ] 每个主题的 README 中，规律描述是否覆盖了同一类机制？
- [ ] 是否有具体接口、URL 或文件被错误地当成规律？
- [ ] 每个例外是否有明确边界（文件路径、条件、触发场景）？
- [ ] 单点事实是否确实无法归入任何规律或例外？
- [ ] 输出是否直接服务于安全测试（攻击面识别、绕过点定位、关键配置查找）？
- [ ] 是否存在可以删除而不影响安全测试理解的冗余内容？
