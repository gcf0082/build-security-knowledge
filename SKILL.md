---
name: build-security-knowledge
description: 分析代码仓,自动生成供安全测试使用的知识库(目录树形式的 markdown)。知识库聚合五类安全测试最需要的"规律":URL→代码映射规律、外部/内部接口识别规律、接口认证鉴权规律、限流规律、沙箱机制及其关键配置。当用户要求为代码仓生成安全测试知识库、攻击面地图、接口路由梳理、内外接口识别、认证矩阵/免认证清单、限流分析、沙箱机制分析时,即使只提到其中一项,也应使用本 skill。
---

# build-security-knowledge

## 这个 skill 的价值

安全测试者拿到一个陌生代码仓,最需要的是五类**可推导的规律**,而不是一份接口清单:

- 任意 URL → 它映射到哪段代码
- 任意接口 → 它是外部还是内部
- 任意接口 → 过不过认证、鉴权怎么判
- 任意接口 → 受不受限流
- 这个仓 → 有没有沙箱,关键配置是什么

有了规律,测试者能自行推导新接口,把精力集中在真正需要手工测的点上。

## 四条铁律

1. **可溯源**:每条规律和事实给出 `file:line`。找不到出处 = 不可信。
2. **不臆造**:无法从代码确定的,写"待人工核实"并给出怀疑点。宁可标注,绝不硬造结论。
3. **只写安全测试要用的**:接口清单只作为规律的载体,不写业务功能描述、字段级细节等普通文档内容。
4. **普适性优先,单点是例外**:知识库的价值在于**对项目整类生效**的规律,而非单点观察。单个端点的发现只是原料,必须回推成全类通用的规则(配合例外清单刻画边界);若只找到孤立事实、推不出通用规律,必须标注"单点事实,规律未确认",不能冒充规律。

## 工作流

### Step 1:识别技术栈(先做,不要通读)

采用"配置优先、由表及里"的探测方法:优先从构建/部署配置判断语言、框架与网关,再按已知框架的特征模式进入源码定位关注点,而非盲目通读代码。框架识别信号与各框架的关注模式见 references/stack-patterns.md;识别不出已知框架时,按其中"通用模式"的启发信号探测。只把有代码依据的结论写入知识库,其余一律标"待人工核实"。

### Step 2:逐维度提炼规律

每个维度遵循:找到判定依据代码 → 归纳成规律 → 列出例外 → 标出未确认。规律是"凡满足 A 模式的都归为 X",不是逐接口枚举。

**归纳方法(普适性的关键)**:先收集多个实例 → 找出它们的共同模式 → 提炼为整类规律 → 例外清单成为规律边界。例如不要写"登录接口无认证",要写"所有 `/api/auth/**` 前缀接口都未纳入认证拦截,因为 SecurityConfig.java:41 对该前缀 `permitAll`"。每个维度产出的结论都必须能用"凡……都……"表述;做不到就如实标注"仅发现单点事实,规律未确认"。

**多规律并存(大型项目的常态)**:同一维度在大型项目中往往有**多条规律**,不能强行合并成一条。按"范围"划分规律——每个模块/服务/前缀/层次可能有各自的规律:
- 每条规律必须**声明自己的适用范围**(如"`user-svc` 模块""`/internal/**` 前缀""网关层""应用层"),再给出规律表述与依据。
- 规律之间范围可以重叠(如应用层的业务接口一个规律、`/internal` 管理接口另一个规律),重叠时要写明。
- 输出时按规则组组织,每个规则组头部用一句话概括该组规律(类似各文件的"一句话结论")。

#### 维度 1:URL→代码 映射规律(`01-url-mapping`)

产出让测试者**拿着 URL 能推导到代码**的规律:
- 路由注册方式:注解 / 路由注册表 / 配置文件 / 约定式路径
- 网关/nginx 的 `location` 与 `rewrite` 规律(URL 前缀 → 上游服务)
- 统一前缀 ↔ 模块/服务 的对应关系
- path 变量、静态资源、兜底/error 路由的规律

#### 维度 2:外部/内部接口识别规律(`02-interface-classify`)

产出判定内外接口的**规律与判定链**:
- 路径约定:`/internal` `/admin` `/actuator` 等
- 绑定地址:127.0.0.1/内网 vs 0.0.0.0
- 网关 `location` 是否对外转发
- 服务注册 / 内部 RPC 协议(微服务仓常见)
- 无法按规律判定的接口 → 列出"待人工核实"

#### 维度 3:接口认证鉴权规律(`03-auth`)

- **认证覆盖规律**:默认是否全局拦截;拦截器/中间件/过滤器在哪儿;免认证/放行接口及其**配置位置**(这是安全测试的重点)
- **鉴权规律**:角色/权限点在哪声明、在哪判定
- 常见形态 e.g."默认全部需要认证,仅 X/Y 例外,白名单在 `xxx.yml`"

#### 维度 4:限流规律(`04-rate-limit`)

- 限流在哪层:网关(nginx `limit_req`) / 应用中间件 / 注解
- key 是什么:IP / uid / token / 接口
- 阈值与窗口期配置在哪
- 若确无限流:明写"未发现限流机制"(同样有价值,测试者需要知道)

#### 维度 5:沙箱机制 + 关键配置(`05-sandbox`)

- 先答"有没有":是否存在脚本引擎、动态代码执行、模板渲染用户输入、文件/容器级隔离
- 隔离边界在哪,做了什么限制
- 关键配置:白名单(类/函数/URL)、超时、资源上限、与逃逸相关的开关
- 若发现动态执行点但无隔离:明写"存在执行点,未发现沙箱",并点名位置与依据 `file:line`

### Step 3:输出知识库

按 references/output-template.md 的模板,在**当前目录**生成 `_security-knowledge/` 目录树。目录**动态生成**:某维度确有关键内容(哪怕结论是"该机制不存在")就建对应文件;确属无关才跳过,并在 README 的"覆盖情况"一节说明。

## 规模控制

- **大型仓库分层推进**:先把握全局结构(模块/服务划分),再按范围逐层提炼规律,由结构到细节逐层深入,避免一次性读完整仓。优先产出各范围的粗粒度规律,再针对高价值或高不确定的范围细化。
- 大型项目若同一维度规律过多,允许在 `_security-knowledge/` 下按模块拆成子目录(如 `03-auth/` 下 `user-svc.md` `order-svc.md`),并在上级 README 汇总索引。
- 全仓无内容的维度不硬写,统一在 README 标注。